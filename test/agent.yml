name: agent
tags:
  - ai
  - telegram
  - miniclaw

# Input for context (e.g., poll telegram)
src: tg://resolve?domain=YOUR_BOT_NAME&start=listen_poll_ID123

# AI agent command (e.g., claude CLI) in a script.
# claude --dangerously-skip-permissions ...
run: claude.sh 

# Output for the agent's response (e.g., message telegram)
dst: tg://msg?to=@joe
